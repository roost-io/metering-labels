# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-labels/{metering_label_id}_delete for http method type DELETE in artillery framework
# RoostTestHash=b1a615d400



# ********RoostGPT********
config:
  target: {{ $processEnvironment.API_HOST }}
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    file: metering-labels_metering_label_id_delete_success.csv
    skipHeader: true
scenarios:
  - flow:
      - get:
          url: "/v2.0/metering/metering-labels/{{metering_label_id}}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: application/json
            - hasProperty: id
            - hasProperty: name
            - hasProperty: created_at
            - hasProperty: updated_at
            - hasProperty: deleted_at
            - hasProperty: is_deleted
      - think: 1000
      - delete:
          url: "/v2.0/metering/metering-labels/{{metering_label_id}}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 204
      - think: 1000
