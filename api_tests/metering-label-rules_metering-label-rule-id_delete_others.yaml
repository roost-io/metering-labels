# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-label-rules/{metering-label-rule-id}_delete for http method type DELETE in artillery framework
# RoostTestHash=6da8381989



# ********RoostGPT********
config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

scenarios:
  - scenario: Failure cases for metering label rule deletion
    - delete:
        url: "/v2.0/metering/metering-label-rules/123"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        think: 500
        expect:
          statusCode: 404
    - delete:
        url: "/v2.0/metering/metering-label-rules/87654321"
        think: 500
        expect:
          statusCode: 401
    - delete:
        url: "/v2.0/metering/metering-label-rules/87654321"
        headers:
          Authorization: "Bearer invalid-token"
        think: 500
        expect:
          statusCode: 401
    - delete:
        url: "/v2.0/metering/metering-label-rules/non-existent-id"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        think: 500
        expect:
          statusCode: 404
    - delete:
        url: "/v2.0/metering/metering-label-rules/"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        think: 500
        expect:
          statusCode: 404
    - delete:
        url: "/invalid/url"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        think: 500
        expect:
          statusCode: 404
