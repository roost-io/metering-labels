# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-labels_post for http method type POST in artillery framework
# RoostTestHash=b9a8e69447



# ********RoostGPT********
---
title: "Metering Labels - Create Success"
config:
  target: "{{ $processEnvironment.API_HOST }}"
  expect: true
  payload:
    file: metering-labels_post_success.csv
    skipHeader: true
scenarios:
  - flow:
      - get:
          url: "/v2.0/metering/metering-labels"
          name: "Get Metering Labels"
          headers:
            authorization: Bearer {{ $processEnvironment.AUTH_TOKEN }}
          expect:
            statusCode: 200
            contentType: application/json
            hasHeader: content-range
            jmespath:
              - '[?name == `label1`]'
      - think: 500
      - post:
          url: "/v2.0/metering/metering-labels"
          name: "Create Metering Label"
          headers:
            authorization: Bearer {{ $processEnvironment.AUTH_TOKEN }}
            content-type: application/json
          payload:
            tenant_id: {{ tenant_id }}
            description: {{ description }}
            name: {{ name }}
          expect:
            statusCode: 201
            contentType: application/json
            hasHeader: content-range
            hasProperty:
              - id
              - tenant_id
              - description
              - name
      - think: 500
