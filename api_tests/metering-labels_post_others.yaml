# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-labels_post for http method type POST in artillery framework
# RoostTestHash=b9a8e69447



# ********RoostGPT********
config:
  target: {{ $processEnvironment.API_HOST }}
  phases:
    - duration: 10s
      arrivalRate: 10
  payload:
    metering-labels_post_others.csv:
      skipHeader: true
scenarios:
  Negative-Metering-Label-Scenarios:
    - think: 500
      # Invalid URL
      - request:
          method: POST
          url: /v2.0/metering/metering-labels/invalidURL
          expect:
            statusCode: 404

    - think: 500
      # Missing required field
      - request:
          method: POST
          url: /v2.0/metering/metering-labels
          payload:
            meteringLabelName: missingRequiredField
          expect:
            statusCode: 400

    - think: 500
      # Missing required field
      - request:
          method: POST
          url: /v2.0/metering/metering-labels
          headers:
            token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 400

    - think: 500
      # Invalid data type
      - request:
          method: POST
          url: /v2.0/metering/metering-labels
          payload:
            meteringLabelName: 123
          expect:
            statusCode: 400

    - think: 500
      # Invalid data type
      - request:
          method: POST
          url: /v2.0/metering/metering-labels
          payload:
            meteringLabelName: invalidName
          headers:
            token: {{invalidToken}}
          expect:
            statusCode: 401

    - think: 500
      # Invalid data type
      - request:
          method: POST
          url: /v2.0/metering/metering-labels
          payload:
            meteringLabelName: invalidName
          headers:
            token: {{$processEnvironment.AUTH_TOKEN}}
            invalidHeader: invalidValue
          expect:
            statusCode: 400

    - think: 500
      # Invalid data type
      - request:
          method: POST
          url: /v2.0/metering/metering-labels
          payload:
            meteringLabelName: invalidName
          headers:
            token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 400
            notHasProperty: invalidProperty
