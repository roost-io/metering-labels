# ********RoostGPT********
# Test generated by RoostGPT for test metering-integration using AI Type Azure Open AI and AI Model roost-gpt4-32k
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}/v2.0/metering/metering-label-rules"
  plugins:
    expect: {}
  phases:
    - duration: 10
      arrivalRate: 1
  payload:
    path: "scenario-371ffe8296.csv"
    fields:
      - meteringLabelRuleId
    skipHeader: true
  environments:
    dev:
      API_HOST: "http://dev.example.com"
      AUTH_TOKEN: "Bearer devtoken"

scenarios:
  - name: "Delete Metering Label Rule"
    flow:

      # Check if the metering label rule exists before deletion
      - get:
          url: "/{{ meteringLabelRuleId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "meteringLabelRuleId"
        
      # Think time to simulate user's decision making
      - think: 1

      # Send a DELETE request to delete metering label rule and expect status 204
      - delete:
          url: "/{{ meteringLabelRuleId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 204
