# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-labels_get for http method type GET in artillery framework
# RoostTestHash=3dcb871c88



# ********RoostGPT********
---
# Artillery test script to validate successful API requests for metering labels.

# Environment configuration
config:
  target: {{ $processEnvironment.API_HOST }}

# Payload management
payload:
  file: metering-labels_get_success.csv
  skipHeader: true

# Test scenario
scenarios:
  # Scenario: Validate successful GET requests for metering labels
  get-metering-labels:
    flow:
      # Request: Get all metering labels
      - get:
          # Comment: Sends a GET request to retrieve all metering labels.
          url: /v2.0/metering/metering-labels
          expect:
            # Assert the response status code is 200.
            statusCode: 200
            # Assert the presence of the 'Content-Type' header.
            hasHeader: Content-Type
            # Assert the 'Content-Type' header value is 'application/json'.
            headerEquals: application/json
            # Assert the response body contains the 'metering_labels' property.
            hasProperty: metering_labels
            # Assert each 'metering_labels' property matches the expected schema.
            - metering_labels:
                - hasProperty: id
                - hasProperty: tenant_id
                - hasProperty: description
                - hasProperty: name

# Enable the 'expect' plugin
plugins:
  - expect
