# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-labels/{metering_label_id}_delete for http method type DELETE in artillery framework
# RoostTestHash=b1a615d400



# ********RoostGPT********
config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  payload:
    file: metering-labels_metering_label_id_delete_others.csv
    skipHeader: true

scenarios:
  - scenario: Failure Response Scenarios for Delete Metering Label
    # Invalid URL
    - get:
        url: /v2.0/metering/metering-labels/abc/def
        thinkTime: 1000
        expect:
          statusCode: 404

    # Invalid metering_label_id
    - get:
        url: /v2.0/metering/metering-labels/abc
        thinkTime: 1000
        expect:
          statusCode: 400

    # Missing metering_label_id
    - get:
        url: /v2.0/metering/metering-labels/
        thinkTime: 1000
        expect:
          statusCode: 404

    # Invalid authorization token
    - get:
        url: /v2.0/metering/metering-labels/{{ $processEnvironment.METERING_LABEL_ID }}
        headers:
          Authorization: Bearer {{ $processEnvironment.AUTH_TOKEN }}_INVALID
        thinkTime: 1000
        expect:
          statusCode: 401

    # Missing authorization token
    - get:
        url: /v2.0/metering/metering-labels/{{ $processEnvironment.METERING_LABEL_ID }}
        thinkTime: 1000
        expect:
          statusCode: 401
