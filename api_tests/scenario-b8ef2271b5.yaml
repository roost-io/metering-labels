# ********RoostGPT********
# Test generated by RoostGPT for test metering-integration using AI Type Azure Open AI and AI Model roost-gpt4-32k
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}/v2.0/metering"
  plugins:
    expect: {}
  stages:
    - duration: 1
      arrivalRate: 1
  payload:
    path: "scenario-b8ef2271b5.csv"
    fields:
      - "remote_ip_prefix"
      - "direction"
      - "metering_label_id"
      - "id"
      - "excluded"
    skipHeader: true

scenarios:

  # Scenario: Show Metering Label Rule
  - name: "Show Metering Label Rule"
    flow:

      - get:
          url: "/metering-label-rules/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "metering_label_id"
            - hasProperty: "direction" 
            - hasProperty: "remote_ip_prefix"
            - hasProperty: "excluded"
          log: "GET /v2.0/metering/metering-label-rules/{{id}} should return the metering label rule"
          capture:
            json: "$"
            as: "response"
      - think: 1
      # Assert the details of the metering label rule in the response
      # Check if the values in response are equal to the request
      - assert:
          - equals: "response.id"
            expect: "{{ id }}"
          - equals: "response.metering_label_id"
            expect: "{{ metering_label_id }}"
          - equals: "response.direction"
            expect: "{{ direction }}"
          - equals: "response.remote_ip_prefix"
            expect: "{{ remote_ip_prefix }}"
          - equals: "response.excluded"
            expect: "{{ excluded }}"
