# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-label-rules_post for http method type POST in artillery framework
# RoostTestHash=067b623f58



# ********RoostGPT********
---
config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  payload:
    file: metering-label-rules_post_success.csv
    skipHeader: true
    fields:
      - metering_label_rule.remote_ip_prefix
      - metering_label_rule.direction
      - metering_label_rule.metering_label_id
      - metering_label_rule.excluded
scenarios:
  Metering-Label-Rules-Create-Success:
    flow:
      - get:
          url: /v2.0/metering/metering-labels
          authorization:
            bearer: {{ $processEnvironment.AUTH_TOKEN }}
      - post:
          url: /v2.0/metering/metering-label-rules
          headers:
            Content-Type: application/json
          authorization:
            bearer: {{ $processEnvironment.AUTH_TOKEN }}
          payload:
            metering_label_rule:
              remote_ip_prefix: {{ metering_label_rule.remote_ip_prefix }}
              direction: {{ metering_label_rule.direction }}
              metering_label_id: {{ metering_label_rule.metering_label_id }}
              excluded: {{ metering_label_rule.excluded }}
          expect:
            statusCode: 201
            contentType: application/json
            body:
              hasProperty: metering_label_rule
              hasProperty: metering_label_rule.id
              hasProperty: metering_label_rule.remote_ip_prefix
              hasProperty: metering_label_rule.direction
              hasProperty: metering_label_rule.metering_label_id
              hasProperty: metering_label_rule.excluded
              equals:
                metering_label_rule.remote_ip_prefix: {{ metering_label_rule.remote_ip_prefix }}
                metering_label_rule.direction: {{ metering_label_rule.direction }}
                metering_label_rule.metering_label_id: {{ metering_label_rule.metering_label_id }}
                metering_label_rule.excluded: {{ metering_label_rule.excluded }}
      - think: 500
