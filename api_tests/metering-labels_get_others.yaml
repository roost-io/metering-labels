# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-labels_get for http method type GET in artillery framework
# RoostTestHash=3dcb871c88



# ********RoostGPT********
# Artillery YAML Test Script
---
config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

scenarios:
  - name: Failure Scenarios
    flow:
      # Invalid URL
      - get:
          url: /v2.0/metering/metering-labels/invalid
          thinkTime: 500
        expect:
          statusCode: 404
          notHasProperty:
            - id
            - name
            - description

      # Missing Authorization Header
      - get:
          url: /v2.0/metering/metering-labels
          thinkTime: 500
        expect:
          statusCode: 401
          notHasProperty:
            - id
            - name
            - description

      # Invalid Authorization Token
      - get:
          url: /v2.0/metering/metering-labels
          headers:
            Authorization: Bearer invalid_token
          thinkTime: 500
        expect:
          statusCode: 401
          notHasProperty:
            - id
            - name
            - description

      # Invalid JSON Payload (POST)
      - post:
          url: /v2.0/metering/metering-labels
          headers:
            Authorization: Bearer {{ $processEnvironment.AUTH_TOKEN }}
          payload:
            invalid_field: invalid_value
          thinkTime: 500
        expect:
          statusCode: 400
          hasProperty:
            - message

      # Missing Required Field (PUT)
      - put:
          url: /v2.0/metering/metering-labels/1
          headers:
            Authorization: Bearer {{ $processEnvironment.AUTH_TOKEN }}
          payload:
            name: Updated Label
          thinkTime: 500
        expect:
          statusCode: 400
          hasProperty:
            - message

      # Non-Existent ID (DELETE)
      - delete:
          url: /v2.0/metering/metering-labels/999
          headers:
            Authorization: Bearer {{ $processEnvironment.AUTH_TOKEN }}
          thinkTime: 500
        expect:
          statusCode: 404
          notHasProperty:
            - id
            - name
            - description
