# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-label-rules_get for http method type GET in artillery framework
# RoostTestHash=67255cbbcd



# ********RoostGPT********
---
# Artillery test script for Metering Label Rules API
config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

scenarios:
  # Scenario: Get Metering Label Rules (Success)
  - flow:
      # GET /v2.0/metering/metering-label-rules
      - get:
          - url: /v2.0/metering/metering-label-rules
            headers:
              Authorization: Bearer {{ $processEnvironment.AUTH_TOKEN }}
            expect:
              statusCode: 200
              contentType: application/json
              hasProperty: metering_label_rules
              hasProperty: metering_label_rules[0].remote_ip_prefix
              hasProperty: metering_label_rules[0].direction
              hasProperty: metering_label_rules[0].metering_label_id
              hasProperty: metering_label_rules[0].id
              hasProperty: metering_label_rules[0].excluded
      # Think time of 1 second between requests
      - think: 1000
