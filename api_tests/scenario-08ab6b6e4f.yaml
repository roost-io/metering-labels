# ********RoostGPT********
# Test generated by RoostGPT for test metering-integration using AI Type Azure Open AI and AI Model roost-gpt4-32k
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}/v2.0/metering"
  plugins:
    expect: {}
  payloads:
    - path: "scenario-08ab6b6e4f.csv"
      fields:
        - "id"
        - "tenant_id"
        - "description"
        - "name"
      skipHeader: true

scenarios:
  - name: "List Metering Labels"
    flow:
      - get:
          # URL stays in-line with the scenario
          url: "/metering-labels" 
          headers:
            # using authorization from environment
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            # Expected status code 200 as given in scenario 
            - statusCode: 200
            # Field assertion as per scenario
            - hasProperty: "id"
            - hasProperty: "tenant_id"
            - hasProperty: "description"
            - hasProperty: "name"
            # Values assertion
            - equals:
              - "id": "{{ id }}"
              - "tenant_id": "{{ tenant_id }}"
              - "description": "{{ description }}"
              - "name": "{{ name }}"
          # Added log statement to understand the flow
          log: "Retrieve list of metering labels"

      - think: 1
