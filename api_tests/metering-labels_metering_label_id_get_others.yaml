# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-labels/{metering_label_id}_get for http method type GET in artillery framework
# RoostTestHash=a7069bdb0c



# ********RoostGPT********
---
config:
  target: {{ $processEnvironment.API_HOST }}
  expect:
    include:
      - 'expect-http'
  #Payload CSV
  payload:
    file: metering-labels_metering_label_id_get_others.csv
    skipHeader: true
    # Payload Field
    fieldNames:
      - meteringLabelId
  # Environment Variable for Auth token
  variables:
    token: {{ $processEnvironment.AUTH_TOKEN }}
scenario:
  # Invalid Metering Label ID
  - get:
      comment: 'Retrieve a metering label with an invalid metering_label_id (404)'
      url: /v2.0/metering/metering-labels/999999999
      statusCode: 404
      # Think Time
      think: 500
  # No Authorization
  - get:
      comment: 'Access a metering label without authorization (401)'
      url: /v2.0/metering/metering-labels/{{ meteringLabelId }}
      statusCode: 401
      # Think Time
      think: 500
  # Invalid Authorization Token
  - get:
      comment: 'Retrieve a metering label with an invalid authorization token (401)'
      url: /v2.0/metering/metering-labels/{{ meteringLabelId }}
      headers:
        Authorization: Bearer {{ 'invalid-token' }}
      statusCode: 401
      # Think Time
      think: 500
  # Invalid URL
  - get:
      comment: 'Attempt to access an invalid URL (404)'
      url: /invalid/url
      statusCode: 404
      # Think Time
      think: 500
