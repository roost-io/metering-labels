# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-labels/{metering_label_id}_get for http method type GET in artillery framework
# RoostTestHash=a7069bdb0c



# ********RoostGPT********
config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

scenarios:
  - name: Failure Scenario
    flow:
      - get:
          description: |
            Send a GET request to a non-existent metering label ID.
            Expect a 404 Not Found response.
          url: /v2.0/metering/metering-labels/9999999999
          expect:
            statusCode: 404
      - post:
          description: |
            Send a POST request with an invalid payload.
            Expect a 400 Bad Request response.
          url: /v2.0/metering/metering-labels
          payload:
            # Invalid payload
            invalid_field: "invalid value"
          headers:
            authorization: Bearer {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 400
      - patch:
          description: |
            Send a PATCH request with an invalid payload.
            Expect a 400 Bad Request response.
          url: /v2.0/metering/metering-labels/1
          payload:
            # Invalid payload
            invalid_field: "invalid value"
          headers:
            authorization: Bearer {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 400
      - delete:
          description: |
            Send a DELETE request to a non-existent metering label ID.
            Expect a 404 Not Found response.
          url: /v2.0/metering/metering-labels/9999999999
          headers:
            authorization: Bearer {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 404
      - get:
          description: |
            Send a GET request with an invalid URL.
            Expect a 404 Not Found response.
          url: /invalid/url
          expect:
            statusCode: 404
      - think: 500
