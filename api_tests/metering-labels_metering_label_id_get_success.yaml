# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Vertex AI and AI Model code-bison-32k

# Test generated for /v2.0/metering/metering-labels/{metering_label_id}_get for http method type GET in artillery framework
# RoostTestHash=a7069bdb0c



# ********RoostGPT********
# Artillery test script for Metering Label API (GET) - Success scenarios

config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

scenarios:
  - flow:
      - post:
          # Post a new metering label
          url: /v2.0/metering/metering-labels
          payload:
            tenant_id: {{ $processEnvironment.TENANT_ID }}
            name: "New Metering Label"
            description: "This is a new metering label."
          authorization:
            type: bearer
            token: {{ $processEnvironment.AUTH_TOKEN }}
      - get:
          # Retrieve the newly created metering label
          url: /v2.0/metering/metering-labels/{{ metering_label_id }}
          authorization:
            type: bearer
            token: {{ $processEnvironment.AUTH_TOKEN }}
      - think: 500 # Think time of 500ms between requests
