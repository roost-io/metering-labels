# ********RoostGPT********
# Test generated by RoostGPT for test metering-integration using AI Type Azure Open AI and AI Model roost-gpt4-32k
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}/v2.0/metering"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "scenario-f87b37cf63.csv"
    fields:
      - "meteringLabelId"
      - "authorizationToken"
    skipHeader: true
    
scenarios:

# Test scenario to get metering labels with given metering label ID
  - name: "Show Metering Label"
    flow:
    # Log the action of sending a GET request
      - log: "Sending GET request to /metering-labels/{{ meteringLabelId }}"
    # Define the GET request
      - get:
          url: "/metering-labels/{{ meteringLabelId }}"
          headers:
            Authorization: "Bearer {{ authorizationToken }}"
          expect:
          # Assert status code
            - statusCode: 200
          # Assert properties of response
            - hasProperty: 'tenant_id'
            - hasProperty: 'description'
            - hasProperty: 'name'
          # Assert the values of response
            - equals: 
                - "id"
                - "{{ meteringLabelId }}"
            - equals: 
                - "tenant_id"
                - "45345b0ee1ea477fac0f541b2cb79cd4"
            - equals: 
                - "description"
                - "label1 description"
            - equals: 
                - "name"
                - "label1"
      # Pause for a second before the next iteration
      - think: 1
