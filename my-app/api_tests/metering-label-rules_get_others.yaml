# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /v2.0/metering/metering-label-rules_get for http method type GET in artillery framework
# RoostTestHash=67255cbbcd



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 30
      arrivalRate: 10
  payload:
    path: "metering-label-rules_get_others.csv"
    fields: 
      - id
      - name
    skipHeader: true
  plugins:
    expect: {} 
  environments:
    e2e:
      target: 'http://localhost:5000'
      
scenarios:
  - name: Testing non-success API responses
    flow:
      # Sending GET request with invalid URL
      - get:
          url: "/v2.0/meterings/metering-label-rule/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect: 
            - statusCode: 404
          think: 1
              
      # Sending GET request with invalid token
      - get:
          url: "/v2.0/metering/metering-label-rules/{{ id }}"
          headers:
            Authorization: "Bearer incorrect_token"
          expect:
            - statusCode: 401
          think: 1

      # Sending GET request with missing token
      - get:
          url: "/v2.0/metering/metering-label-rules/{{ id }}"
          headers:
            Authorization: ""
          expect:
            - statusCode: 403
          think: 1

      # Sending GET request with valid parameters but expect JSON response when API produces only XML
      - get:
          url: "/v2.0/metering/metering-label-rules/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Accept: "application/json"
          expect:
            - statusCode: 406  # Not Acceptable
          think: 1   
