# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /v2.0/metering/metering-label-rules_get for http method type GET in artillery framework
# RoostTestHash=67255cbbcd



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "metering-label-rules_get_success.csv"
    fields:
      - "metering_label_id"
      - "rule_id"
    skipHeader: true
  environments:
    dev:
      target: "https://dev.api.mysite.com"

scenarios:
  - name: "Valid API Test Scenario"
    flow:
      - get:
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          url: "/v2.0/metering/metering-label-rules"
          expect: 
            - statusCode: 200
            - hasProperty: "metering_label_rules"
        think: 1
      - get:
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          url: "/v2.0/metering/metering-label-rules/{{ metering_label_id }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "direction"
            - hasProperty: "remote_ip_prefix"
            - hasProperty: "excluded"
        think: 1
      - get:
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          url: "/v2.0/metering/metering-label-rules/{{ rule_id }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "direction"
            - hasProperty: "remote_ip_prefix"
            - hasProperty: "excluded"
        think: 1
