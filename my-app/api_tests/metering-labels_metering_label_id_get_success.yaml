# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /v2.0/metering/metering-labels/{metering_label_id}_get for http method type GET in artillery framework
# RoostTestHash=a7069bdb0c



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  phases:
    - duration: 60
      arrivalRate: 5
      rampTo: 50
      name: "Warm up the application"
    - duration: 120
      arrivalRate: 50
      name: "Sustained load"
  plugins:
    expect: {}
  payload:
    path: "metering-labels_metering_label_id_get_success.csv"
    fields:
      - "id"
      - "description"
      - "name"
    skipHeader: true
  headers:
    Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Validate correct API behavior with valid input payloads"
    flow:
      - get:
          url: "/v2.0/metering/metering-labels/{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: "metering_label"
            - hasProperty: "metering_label.id"
            - equals: ["metering_label.id", "{{ id }}"]
            - hasProperty: "metering_label.tenant_id"
            - hasProperty: "metering_label.description"
            - equals: ["metering_label.description", "{{ description }}"]
            - hasProperty: "metering_label.name"
            - equals: ["metering_label.name", "{{ name }}"]
        think: 1
