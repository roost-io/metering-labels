# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /v2.0/metering/metering-labels/{metering_label_id}_delete for http method type DELETE in artillery framework
# RoostTestHash=b1a615d400



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p99: 200
  phases:
    - duration: 60
      arrivalRate: 2
      name: "Steady load"
  payload:
    path: "metering-labels_metering_label_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
    
scenarios:
  - name: "Delete metering label"
    flow:
      # First request to delete metering label
      - delete: "/v2.0/metering/metering-labels/{{ id }}"
        headers:
          Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 204
      # Think time
      - think: 5
