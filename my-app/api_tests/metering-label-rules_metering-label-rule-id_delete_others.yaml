# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /v2.0/metering/metering-label-rules/{metering-label-rule-id}_delete for http method type DELETE in artillery framework
# RoostTestHash=6da8381989



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payloads:
    - name: "delete_others"
      path: "metering-label-rules_metering-label-rule-id_delete_others.csv"
      skipHeader: true
      fields:
        - "id"
        - "invalid_id"
        - "n_auth_token"

scenarios:
  - name: "Invalid API usage scenario"
    flow:
      # Invalid ID case
      - think: 3
      - post:
          url: "/v2.0/metering/metering-label-rules/{{ invalid_id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            
      # No authentication token case
      - think: 3
      - post:
          url: "/v2.0/metering/metering-label-rules/{{ id }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"

      # Invalid URL case
      - think: 3
      - post:
          url: "/invalidurl/metering/metering-label-rules/{{ id }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            
      # Valid request but expecting incorrect response
      - think: 3
      - post:
          url: "/v2.0/metering/metering-label-rules/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - notHasProperty: "error"

