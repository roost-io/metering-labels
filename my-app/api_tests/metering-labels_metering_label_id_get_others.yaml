# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /v2.0/metering/metering-labels/{metering_label_id}_get for http method type GET in artillery framework
# RoostTestHash=a7069bdb0c



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 300
      arrivalRate: 20
  payload:
    path: "metering-labels_metering_label_id_get_others.csv"
    fields:
      - "metering_label_id"
    skipHeader: true
  processor: "./myFunction.js"
  plugins:
    expect: {}

scenarios:
  - name: "Unsuccessful API Requests Test Suite"
    weight: 100
    flow:
      # Test Case 1 - GET Request with invalid metering_label_id
      - get:
          url: "/v2.0/metering/metering-labels/INVALID_ID"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 1

      # Test Case 2 - GET Request with no Authorization token
      - get:
          url: "/v2.0/metering/metering-labels/{{ metering_label_id }}"
          expect:
            - statusCode: 401
        think: 1

      # Test Case 3 - GET Request with incorrect URL
      - get:
          url: "/v2.0/metering/metering/INVALID_URL"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1

      # Test Case 4 - GET Request with malformed Authorization token
      - get:
          url: "/v2.0/metering/metering-labels/{{ metering_label_id }}"
          headers:
            Authorization: "Bearer INVALID_TOKEN"
          expect:
            - statusCode: 403
        think: 1
