# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /v2.0/metering/metering-labels_post for http method type POST in artillery framework
# RoostTestHash=b9a8e69447



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
  - duration: 60
    arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "metering-labels_post_success.csv"
    fields:
      - "id"
      - "tenant_id"
      - "description"
      - "name"
    skipHeader: true
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "POST /v2.0/metering/metering-labels"
    flow:
      - post:
          url: "/v2.0/metering/metering-labels"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            label:
              id: "{{ id }}"
              tenant_id: "{{ tenant_id }}"
              description: "{{ description }}"
              name: "{{ name }}"
          expect:
            - statusCode: 201
            - hasProperty: "label.id"
            - hasProperty: "label.tenant_id"
            - hasProperty: "label.description"
            - hasProperty: "label.name"
          think: 1
