# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /v2.0/metering/metering-label-rules/{metering-label-rule-id}_get for http method type GET in artillery framework
# RoostTestHash=987f45854d



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my-script.js"
  ensure:
    p95: 200
  phases:
    - duration: 120
      arrivalRate: 50
  plugins:
    expect: {}
  payload:
    path: "metering-label-rules_metering-label-rule-id_get_others.csv"
    fields:
      - "meteringLabelRuleId"
    skipHeader: true

scenarios:
  - flow:
      # Invalid Metering Label Rule GET Request with Non-Existing ID
      - get:
          url: "/v2.0/metering/metering-label-rules/1234"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1
        
      # Invalid Metering Label Rule GET Request with Invalid ID
      - get:
          url: "/v2.0/metering/metering-label-rules/invalid_id"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
        think: 1

      # Invalid Metering Label Rule GET Request without Authorization
      - get:
          url: "/v2.0/metering/metering-label-rules/{{ meteringLabelRuleId }}"
          expect:
            - statusCode: 401
        think: 1
