# ********RoostGPT********
# Test generated by RoostGPT for test metering-csv using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /v2.0/metering/metering-labels_get for http method type GET in artillery framework
# RoostTestHash=3dcb871c88



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "metering-labels_get_success.csv"
    fields:
      - id
      - tenant_id
      - description
      - name
    skipHeader: true
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
    
scenarios:
  - name: "Test Case: Test metering labels API"
    flow:
      - get:
          url: "/v2.0/metering/metering-labels"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: id
            - hasProperty: tenant_id
            - hasProperty: description
            - hasProperty: name
          think: 1
      - log: "Check metering labels API Success"
